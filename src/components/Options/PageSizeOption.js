import React from 'react';
import Option from './Option';
import {pageOptions} from '../../common/constant';

function PageSizeOption() {
  return <Option options={pageOptions}/>;
}

export default PageSizeOption;
